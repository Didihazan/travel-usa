'use strict';

var Promise = require('bluebird');

module.exports = function () {
  // return 'im promise module';
};
module.exports.init = function () {
  return new Promise(function (resolve) {
    setTimeout(function () {
      // console.log('>>> module promise resolved');
      resolve();
    }, 100);
  });
};
